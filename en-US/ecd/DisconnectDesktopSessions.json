{
  "name": "DisconnectDesktopSessions",
  "protocol": "HTTP|HTTPS",
  "method": "POST|GET",
  "pathPattern": "",
  "parameters": [
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": "地域ID。可以调用[DescribeRegions](~~DescribeRegions~~)获取无影云电脑支持的地域列表。"
    },
    {
      "name": "Sessions",
      "position": "Query",
      "type": "Array",
      "required": true,
      "description": "会话详情。"
    },
    {
      "name": "PreCheck",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": "是否进行预检查。若执行预检查，则不会真正执行断连操作，只会将不符合执行条件的会话列表返回。"
    }
  ]
}