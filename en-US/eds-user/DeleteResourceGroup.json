{
  "name": "DeleteResourceGroup",
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ResourceGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": "待删除的资源组ID。\n\n> 如果`ResourceGroupIds`参数不为空，则两个参数同时生效。"
    },
    {
      "name": "ResourceGroupIds",
      "position": "Query",
      "type": "Array",
      "required": false,
      "description": "待删除的资源组ID列表。\n\n> 如果`ResourceGroupIds`参数不为空，则两个参数同时生效。"
    }
  ]
}