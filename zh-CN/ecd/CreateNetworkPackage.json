{
  "name": "CreateNetworkPackage",
  "protocol": "HTTP|HTTPS",
  "method": "POST|GET",
  "pathPattern": "",
  "parameters": [
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": "地域ID。可以调用[DescribeRegions](~~196646~~)获取无影云电脑支持的地域列表。"
    },
    {
      "name": "Bandwidth",
      "position": "Query",
      "type": "Integer",
      "required": true,
      "description": "公网精品带宽的带宽大小。单位：Mbps。    \n\n- 如果是包年包月精品带宽，则取值范围是2\\~1000。\n- 如果是按量付费精品带宽，且类型是按使用流量计费（PayByTraffic），则取值范围是2\\~200；\n- 如果是按量付费精品带宽，且类型是按固定带宽计费（PayByBandwidth），则取值范围是2\\~1000。"
    },
    {
      "name": "OfficeSiteId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": "办公网络ID。"
    },
    {
      "name": "PayType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": "付费类型。"
    },
    {
      "name": "InternetChargeType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": "公网精品带宽的计费方式。\n\n- 当参数`PayType`取值为`PrePaid`时，取值范围：\n    - PayByBandwidth：按固定带宽计费。\n- 当参数`PayType`取值为`PostPaid`时，取值范围：\n    - PayByTraffic：按使用流量计费。\n    - PayByBandwidth：按固定带宽计费。"
    },
    {
      "name": "Period",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": "包年包月精品带宽的购买时长。当参数`PayType`取值为`PrePaid`时才生效，且为必选值。取值范围由参数`PeriodUnit`的值决定。\n\n- `PeriodUnit`为`Week`时，取值范围为：1。\n- `PeriodUnit`为`Month`时，取值范围为：1、2、3、6。\n- `PeriodUnit`为`Year`时，取值范围为：1、2、3。\n\n默认值：1。"
    },
    {
      "name": "PeriodUnit",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": "包年包月精品带宽的购买时长单位。当参数`PayType`取值为`PrePaid`时才生效，且为必选值。"
    },
    {
      "name": "AutoPay",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": "是否自动支付。"
    },
    {
      "name": "AutoRenew",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": "是否自动续费。"
    },
    {
      "name": "PromotionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": "优惠活动ID。"
    },
    {
      "name": "ResellerOwnerUid",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": ""
    }
  ]
}